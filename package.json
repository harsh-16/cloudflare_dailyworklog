{
  "name": "dailyworklog-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:frontend": "wrangler deploy --config wrangler.frontend.jsonc",
    "db:migrate": "wrangler d1 migrations apply daily-work-log --remote"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
